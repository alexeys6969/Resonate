<UserControl x:Class="Resonate_course_project.Elements.TableItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Resonate_course_project.Elements"
             mc:Ignorable="d" Margin="10,10,5,5">
    <Button Grid.Column="0" Grid.Row="0" Click="OpenTable" Background="#EFEFEF" BorderBrush="Transparent" BorderThickness="0" HorizontalAlignment="Center" VerticalAlignment="Center" Height="199" Width="346">
        <Button.Template>
            <ControlTemplate TargetType="Button">
                <Border CornerRadius="10" 
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
                    <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Top"/>
                </Border>
            </ControlTemplate>
        </Button.Template>

        <StackPanel>
            <Image x:Name="TableIcon" Source="/Images/employeesIcon.png" Height="120" Width="120" HorizontalAlignment="Left" Margin="25,15,15,5"/>
            <TextBlock x:Name="TableName" Text="Сотрудники" Style="{StaticResource HeaderTB}" Foreground="Black" HorizontalAlignment="Left" Margin="25,15,25,5"/>
        </StackPanel>
    </Button>
</UserControl>
